<Window x:Class="BookStore_Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        xmlns:local="clr-namespace:BookStore_Presentation"
        xmlns:UI="clr-namespace:BookStore_Presentation.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=UI:MainWindowViewModel, IsDesignTimeCreatable=True}"
        Title="Bookstore – Administration" Height="450" Width="800">
    <DockPanel>
        <!-- Sidebar -->
        <StackPanel DockPanel.Dock="Left" Width="200" Background="#333">
            <TextBlock Text="📚 Bookstore Administration"
                       Foreground="White"
                       FontWeight="Bold"
                       FontSize="13"
                       Margin="10"/>
            <Button Content="List Inventory by Store" 
                    Margin="10"
                    Command="{Binding OpenInventoryByStoreCommand}"/>
            <Button Content="Add Book to Store" Margin="10"/>
            <Button Content="Remove Book from Store" Margin="10"/>
            <Button Content="View All Books" Margin="10"
                      Command="{Binding OpenBookAdminCommand}"/>
        </StackPanel>

        
        
        
        
        <!-- Main content area -->
        <ContentControl Content="{Binding CurrentViewModel}" />
    </DockPanel>
</Window>